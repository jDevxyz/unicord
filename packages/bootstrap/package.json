{
  "name": "@unicord/bootstrap",
  "version": "1.0.0",
  "description": "A bootstrap of Unicord, the beginning and the end.",
  "author": "BillyAddlers <finnsonalca123@gmail.com>",
  "homepage": "https://j-dev.xyz/projects/unicord",
  "license": "AGPL-3.0",
  "directories": {
    "lib": "src",
    "test": "__tests__"
  },
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@unicord/core": "1.0.0",
    "ts-node": "^8.1.0",
    "typescript": "^3.6.2"
  },
  "devDependencies": {
    "typescript": "^3.6.2"
  }
}
